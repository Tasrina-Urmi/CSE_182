//4.Write a C program to find the area and perimeter of a rectangle.
#include <stdio.h>
int main()
{
    int l,w,p,area;
    l=5;
    w=4;
    p=l+w;
    area=l*w;
    printf("The area of the rectangle is %d\nThe perimeter of the rectangle is %d",area,p);
}
